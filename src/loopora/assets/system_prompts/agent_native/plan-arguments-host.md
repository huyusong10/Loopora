If `$ARGUMENTS` contains `fresh`, create a new candidate and keep old runs only as history. If `$ARGUMENTS` contains a candidate plan file path, submit that file instead of authoring a different one.
