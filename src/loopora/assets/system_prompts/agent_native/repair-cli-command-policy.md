rerun repair_cli_command exactly with --json --compact-json; do not replace it with --json-only and do not pipe Loopora JSON, role wrappers, result wrappers, proof artifacts, or recovery payloads through head, head -c, tail, sed, Python string slicing, or other truncating filters; do not run loopora --help, loopora diagnose, which loopora, find /, read alignment session artifacts or manifests, or search Loopora source/filesystem state as a repair step
