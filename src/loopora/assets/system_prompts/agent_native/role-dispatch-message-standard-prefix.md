Use this exact string as the whole Agent/Task prompt; do not prepend `You are running as`, append `Do the following`, or wrapper examples. Invoke the named host-native role agent; return one raw wrapper JSON object only. Main session writes/submits result. Open local paths. direct .loopora/agent_artifacts; no /tmp staging or wc/count-only probes. Do not paste full CLI JSON/full schemas/large evidence ledgers.
