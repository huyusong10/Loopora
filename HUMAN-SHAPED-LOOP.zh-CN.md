# Human-Shaped Loop：让长期 Agent 任务不靠人反复盯

**简体中文** | [English](./HUMAN-SHAPED-LOOP.md)

本文阐述 Loopora 背后的工程思考。想安装和运行，请参见 [README](./README.zh-CN.md)。

---

Loopora 的出发点很朴素：不想每轮都回来纠偏。

但事情没那么简单——要么偷不了懒，要么不靠谱：

- **开个 `/goal` 让 Agent 持续跑**：跑完才知道偏了多远。Token 花了，结果不可预测，也许要全部重来。
- **写一份超大 PRD 让它照着干**：Agent 很快宣告成功——PRD 越大，注意力越分散，结果越不可靠。它倾向于快速关闭任务，而不是逐条证明。

那不偷懒呢？人每轮都手工纠偏——效果还是不够好：Agent 做 A 忘 B，补 B 丢 C。判断散落在每轮提醒里，流失在轮次之间。同一类话一轮轮被重复说出：

- "除掉 UI，后台也要完整"
- "权限和审计到底做了没有"
- "测试都在主路径上，边界呢"
- "补失败路径"
- "代码膨胀了，该重构了"

这些失败模式不同，根因一样：**判断标准没有稳定位置**。Agent 看不到持久标准，产出就会漂移。

能不能把判断变成结构，在固定位置施加固定影响力？这就是 Human-shaped Loop。

## 1. 为什么 Agent 长任务容易"看起来完成"

退款自助流程——客户管理员在账单页申请退款，有风险的订单转客服。这个任务看起来很适合 Agent：有界面、有业务规则、有测试、有边界情况，一轮很可能不够。

用户提出需求：

> 做一个退款自助流程。做得安全一点，补上测试，迭代到可以交付为止。

第一轮结果看起来不错：页面、表单、状态提示都有了，退款资格判断用了几组模拟规则，主路径测试通过。Agent 回复：

> 退款流程完全开发完毕，达成所有目标。

做 demo 展示流程，这很完整。但正式产品上线，能否交付？

**不能**。因为：

- 权限边界未验证——没证明只有授权客户管理员能发起退款
- 业务逻辑用桩替代——退款资格判断用的是模拟规则
- 边界场景未覆盖——部分退款、争议订单、拒付、超出退款窗口
- 失败路径未设计——支付服务商执行失败时，账务状态怎么变化？客服怎么接管？
- 可审计性未证明——审计日志能不能让客服、财务、合规还原全过程？

开发者让 Agent 继续补。第二轮结果更像产品：页面状态更多、边界规则补了一些，总结里出现了"权限""资格""审计"这些词。

但问题仍在：这一轮到底证明了什么？哪些风险真的被解决，哪些只是被提到？下一轮应该补哪里？

还有一种情况：Agent 做 A 丢 B，补 B 丢 C，或者转向更容易汇报的工作——把权限写进文案、继续用模拟规则、补更多主路径测试，然后说"安全性已加强"。核心风险被更像产品的界面和更圆满的总结盖住了。

问题不是 Agent 不够勤奋，而是正确的判断要靠人一轮一轮手动施加。

## 2. 人每次回来，其实都在问同几件事

人每次介入，看起来提醒的东西都不同。但抽象一下，只有几类：

**这真的完成了吗？**  
"这只是 demo，后台也要完整"

**哪些证据可信？**  
"测试通过了，但边界场景呢？"

**哪些风险不能放过？**  
"权限没证明，不能收尾"

**下一轮应该先补哪里？**  
"先补失败路径，别继续美化 UI"

**现在能不能诚实收尾？**  
"这个风险可以随行，但要有人负责"

这五类判断在不同任务里内容不同，但形态一样：把 Agent 从"看起来完成"拉回"真正交付"。

既然形态稳定，能不能在任务开始前就提取出来，变成后续轮次会自动继承的结构？

这就是 Human-shaped Loop 的核心：把这五类判断编译成执行过程中绕不开的控制点。

## 3. 真正的问题：最终反馈太慢

为什么人必须反复回来？

退款任务的真正难点，不是 Agent 不够努力，而是**最终反馈太慢**。

真正的上线反馈、事故反馈、业务质量反馈，往往在任务跑完很久之后才出现：越权退款是财务对账时才暴露，审计链路缺失是合规审查时才显现，支付失败边界是线上事故后才发现。

等最终反馈到来时，早期偏差已经被后续工作包装得更像完成——更多页面、更多测试、更圆满的总结。

这种"看起来完成，真实问题藏在深处"的状态，是复杂任务的常态。

用例、敏捷迭代、自动化测试擅长的，是把反馈距离缩短到足够快：做一个小切片，跑一次测试，很快知道对不对。它们适合反馈快的系统——改按钮文案、跑一次测试、立刻知道错。

但退款、权限重构、跨服务支付回调这类任务不一样：自动检查仍然重要，只是它们很难独自替代最终反馈。

当最终反馈太晚、错误会级联时，任务需要的是提前选出几个关键风险，在执行过程中不断问：有没有证据？要不要转向？能不能收尾？

更抽象地说，Loopora 做的是慢反馈任务里的前馈治理。

成熟工程流程早就知道这一点。设计评审、阶段门、灰度发布、审计记录，都是因为最终反馈太慢，所以必须在执行过程中插入可决策的检查点。Loopora 不复制组织流程，而是把这类治理能力压缩成任务局部、可审查、可丢弃的 Agent Loop。

## 4. Loopora 做什么：提前放置会改变行动的判断点

Loopora 不是等最后失败了再纠偏。而是在任务开始前，把关键风险、证据标准和收尾规则提前放进循环里。

具体来说：

**运行前：编译判断**  
- 人描述任务和关键判断
- Loopora 提取完成标准、伪完成模式、证据要求、阻断风险
- 编译成一份可审查的 Loop 方案
- 人确认后，Loop 保持稳定

**执行中：证据驱动**  
- Agent 在 Loop 结构里执行
- 每轮结果被整理成证据桶：已证明、弱证据、未证明、阻断风险
- 证据缺口自动牵引下一轮

**收尾时：诚实裁决**  
- 结果清楚区分：哪些已证明、哪些未覆盖、哪些阻断收尾
- 残余风险可见、命名、有人负责
- 系统可以跑完，任务仍可能未被证明——二者分开

以退款任务为例。Loop 把判断整理成：

- 页面提交不是完成
- 权限、资格、支付失败、审计必须有证据
- 越权退款、审计缺失必须拦截
- 罕见支付服务商边界可作为残余风险，但必须可见、有人接管

Agent 跑完第一轮，不能只汇报"我做完了"。它必须回到已明确的判断标准：

- 本轮证明了什么？
- 授权管理员路径有证据吗？
- 退款资格是真业务路径还是模拟规则？

结果被整理成证据桶：

| 证据桶 | 本轮实际情况 |
| --- | --- |
| 已证明 | 页面可提交，主路径测试通过 |
| 弱证据 | 退款资格仍来自模拟规则 |
| 未证明 | 授权管理员路径、支付失败处理、审计链路 |
| 阻断风险 | 越权退款路径没证明安全，不能收尾 |

证据不足时，下一轮不会自由发挥，而是被拉回关键缺口。

## 5. 一个检查点什么时候才真的有用

控制点本身不产生控制能力。

一次评审、一个里程碑、一个裁决点，如果只是记录状态，就是打卡。只有它能让下一轮停下来、换方向、补证据，它才真的有控制力。

有效控制点至少满足三件事：

**它测量关键风险**——不是泛泛的"代码质量"，而是"这次任务最怕哪种伪完成"

**它触发真实决策**——不是标注"需要注意"，而是阻断、转向或收束

**它改变后续行动方向**——下一轮 Agent 注意力投向哪里，是否允许继续写代码，是否必须先补证据

在 Loopora 中，"资源分配"不一定是人力预算，而是后续执行资源：

- 下一轮注意力聚焦哪个缺口
- 是否允许继续写入工作区，还是必须先做只读检查
- 是否必须先补证据才能继续
- 是否阻断收尾、暴露残余风险

只有测量、没有决策、没有转向的节点，只是记录仪式，不是有效控制点。

## 6. 它和 PRD、测试、用例、固定团队模板是什么关系

**PRD 和 prompt**  
回答的是"要做什么"。Human-shaped Loop 回答的是"是否已证明、是否该转向、能否收尾"。不同层面。

PRD 提醒 Agent 应注意什么，但可能被选择性引用。Loop 要求每轮以证据回应。

**测试和自动化检查**  
能写成测试、类型检查、lint、证明脚本的判断，当然应该优先写。这些是硬证据，能自动裁决。

Loopora 补的是另一层：当这些证据缺失、失败、覆盖不足时，Agent 不能把运行完成包装成任务完成。

**用例和敏捷**  
适合反馈快的系统：做一个小切片，立刻知道对错。

Loopora 适合反馈慢的系统：最终反馈太晚、错误会级联、看起来完成不等于真实完成。

**固定团队模板**  
产品经理 Agent → 架构师 Agent → 工程师 Agent → QA Agent，是一种特定形态的 Loop。

它回答"谁先做、谁后做、谁审查谁"，但不回答：
- 这次任务的完成标准是什么？
- 什么证据算充分？
- 哪个缺口应该牵引下一轮？

这些答案随任务改变，固定模板无法自适应。

一句话：固定团队模板是 Loopora 的一种形态，不是本质。

## 7. 什么时候值得用 Loopora

Loopora 不适合所有复杂任务。决定因素不是复杂度，而是**最终反馈是否太慢，以至于必须在执行过程中构造中间判断点。**

判断顺序：

| 门槛 | 如果倾向"是" | 如果倾向"否" |
| --- | --- | --- |
| 一次 Agent 执行加一次人工审阅够不够？ | 直接做更划算 | 继续判断 |
| 最终反馈够不够快？ | 用例或直接 Agent 更合适 | 继续判断 |
| 后续轮次能否构造新证据或中间裁决点？ | 继续判断 | 无需 Loop |
| 判断能不能稳定变成自动检查？ | 优先上测试 | 继续判断 |
| 有没有伪完成风险？ | Loopora 更值得 | 简单循环也许够了 |

例子：
- **通常不需要**：生成活动主题、修堆栈清晰的报错、拆边界明确的函数
- **更适合**：退款自助流程、账单权限重构、跨服务支付回调丢失

关键差别：人类是否需要在最终反馈到来之前，反复回来判断"现在能不能收尾、还差什么证据、下一轮该补哪里"。

## 8. 更强的模型能解决判断力问题吗？

更强的模型当然会让很多事情变简单。它像更资深的工程师，能提前发现更多风险，写出更好的第一版方案。

但没人因为工程师资深，就取消代码评审、测试、上线门禁、审计记录。这不是不信任个人能力，而是交付判断不只存在于个人能力里。

哪些风险可接受，哪些证据算充分，什么时候可以带残余风险上线，都和具体任务、团队承诺、业务环境有关，必须显式暴露出来接受争议。

这也是为什么，一次任务里的判断应该被当作局部的、临时的、可争议的东西：

- 这次退款流程必须保守，不代表每个产品任务都要保守
- 这次可以接受某个残余风险，不代表它是长期偏好

这些判断应该显式、可预览、可修改、可废弃。它们更适合存在于 Agent 外部的 Loop 层，而不是静默写进模型权重。

> 模型学习通用能力，Loop 学习当前任务的判断方式。

## 9. 结论：让人少回来，不是让人消失

未来 AI 与人类的协作，不只会沿着"模型更聪明"这一条线演进。

模型会继续变强，但复杂任务仍然需要人类判断：什么值得做，什么算真实完成，证据是否可信，风险是否可接受，何时继续、停止或换方向。

这套哲学不是从抽象 AI 理论里凭空长出来的。它更像是在学习工程管理中那些经验证明有用的东西：评审、门禁、证据、追溯、复盘，以及对"看起来完成"的天然不信任。

真正的高阶协作，不是每一步都把人拉回，也不是假装人可以完全离开，而是让人类判断力以更合适的时间形态参与任务。

Human-in-the-loop 把人类放在执行过程里。

Human-shaped Loop 把人类判断提前放进循环结构。

**Loopora 想提高的不是 Agent 的自由度，而是可信自治度。自治不是无人约束地继续跑，而是在人的判断结构里继续跑。**

当判断、证据、转向和收尾都被显性化，人才可以真正少回来几次。这就是 Loopora 意义上的偷懒：不是牺牲质量来省事，而是通过提高可信自治度，让同一类判断不必被人重复执行。

这就是 Human-shaped Loop。

如需安装和运行 Loopora，请回到 [README](./README.zh-CN.md)。