# Loopora Contracts

These contracts sit below `HUMAN-SHAPED-LOOP.zh-CN.md` and `README.zh-CN.md`. They define what must stay true while the
implementation stays free to shrink.

## Test Asset Governance

Contract tests protect stable claims through one canonical proof layer plus small cross-layer smoke checks. New tests should
reuse the canonical proof instead of restating the same shape in CLI, Web, service, and adapter assertions.

| Stable claim | Canonical proof layer | Secondary smoke layer | Avoid |
| --- | --- | --- | --- |
| v3 Agent envelope shape | `agent_native_v3_helpers.assert_agent_v3_envelope` and Agent Native summary/projection tests | CLI JSON status/actionability checks | Duplicated envelope helpers, treating `raw.legacy` as the v3 contract |
| Agent Native work panel, proof/lifecycle split, submit repair | Agent Native projection/submit-repair contract tests | Plain first-screen actionability and command smoke | Full prose locking, re-deriving proof semantics in CLI tests |
| Adapter managed entries and native guidance | Adapter section/semantic-marker tests for Codex, Claude Code, and OpenCode | Init/check smoke and managed-version tests | Long entry snippets as contracts, adapter-specific copy as proof |
| Web projection | Web API projection tests split by projection/file-preview/event/bundle/orchestration/network boundary | Minimal page/journey smoke | Page tests reading raw repository records or DOM/CSS internals |
| Alignment readiness and source context | Alignment readiness/session/source-context contract tests | Web/API stream smoke | Repeating full transcript, exact assistant prose, or executor internals |
| Runtime verdict and evidence coverage | Runtime verdict/evidence coverage tests | CLI/Web run-detail smoke | Treating process success, todo state, or host trace as task proof |
| Legacy Agent summary placement | Single diagnostic placement test through the v3 helper | Real-probe review notes may mention legacy placement | New contract tests asserting `raw.legacy` fields directly |

| Boundary | Stable claim | Primary code | Verification | Not a contract |
| --- | --- | --- | --- | --- |
| Product shape | Loopora preserves task-scoped human judgment across slow-feedback Agent work. The Loop, not a prompt pack or DAG engine, is the primary object: it turns delayed human feedback into evidence-bearing control points that can measure risk, trigger decisions, and change later execution. | `src/loopora/service*.py`, `src/loopora/workflows.py` | README checks, alignment guidance checks, shared compiler/runtime contracts, verdict tests | Role count, exact copy, visual layout |
| Dual entry | Web composer and Agent Native are peer entry surfaces into the same Core. Web is full-function; Agent Native must not grow a second compiler. Agent-returned Web URLs and generated fallback/recovery/submit CLI commands must observe the same Loopora home as the Agent command that created the preview or run. | `service_alignment.py`, `service_agent_adapters.py`, `bundles.py`, `agent_web.py`, `agent_adapters.py`, `service_agent_native.py` | Web READY/run smoke, Agent `/loopora-plan -> /loopora-run`, shared bad-bundle error, Agent Web home identity check, generated command home-preservation test | Web-only observer framing, adapter-specific prose |
| Compiler | The compiler turns task judgment into a reviewable and runnable Loop. Web dialogue, Agent candidate plans, YAML import/export, preview, and run creation all use the same bundle normalization and semantic lint. READY preview must reflect current canonical content, and alignment guidance must reject status-only checkpoints that do not route evidence into decisions, repair, blocking, or accepted residual risk. | `src/loopora/bundles.py`, `service_bundle_assets.py`, `service_alignment.py` | One shared success path and one shared failure path across Web/Agent, alignment guidance checks | UI summaries, traceability aliases, stale validation |
| Context choice | Web and Agent Native use the same workdir context resolver before continuing, improving, or starting fresh. Exact Agent binding may resume automatically; ambiguous or cross-session recovery must surface choices instead of guessing, and Agent recovery choices must include status/hint text plus enough status/timestamp metadata to distinguish similar labels. Status/hint text explains whether the choice resumes active work, starts a READY preview, replays a passed terminal run without new Agent work, continues evidence from an unproven terminal run, still needs review, or needs candidate repair. Terminal run choices expose linked run status plus task verdict status/summary so passed replay and next-evidence continuation are not ambiguous. Runnable choices expose copyable slash and CLI run commands; non-runnable choices withhold run commands and instead expose preview/repair evidence such as `preview_url`, `preview_path`, `validation_error`, `repair_focus`, `next_plan_command`, and plan-file paths when available. | `agent_adapter_context_binding.py`, `agent_entry_candidate.py`, `agent_entry_continuation.py`, `agent_entry_run_projection.py`, `service_alignment.py`, `service_agent_adapters.py`, Web alignment context route | workdir context resolution tests, Agent no-binding recovery tests, terminal recovery verdict tests, recoverable-context command/hint tests | Choosing the newest run by default, silent fresh/reuse switches |
| Bundle | Bundle is a Web-operable exchange file: generate, edit, preview, import, export, replace, delete. It is not a long-term catalog or governance object. UI projections such as summaries, traceability aliases, or diagnostics are derived views. Record deletion and local artifact cleanup are separate; non-critical cleanup failure must be visible as a warning. | `bundles.py`, bundle routes/templates | YAML import/export, preview, run handoff, cleanup warning tests, absence of lineage/governance-card defaults | Lineage, revision identity, cards, dashboard metrics |
| Runtime | Run status and Loop verdict are separate. Evidence refs, coverage, and GateKeeper verdict decide whether the task is proven. Ledger entries keep related evidence refs citable without duplicating the same ref inside one entry, role handoffs keep blocking items unique so downstream users see issue count rather than duplicate derivation paths, a later positive coverage claim without supporting evidence must not promote an already blocked target to Proven, and a later supporting coverage proof replaces historical blocked refs in Proven target evidence refs. Self-healing transient database retries remain in `service.log` diagnostics but do not appear in the default terminal warning stream. | `service_run_*`, `service_agent_native.py`, `task_verdicts.py`, `context_flow.py`, `run_takeaways.py`, `db.py` | verdict/status tests, evidence ledger checks, related evidence ref dedupe check, handoff blocker dedupe check, coverage supporting-ref replacement test, database retry diagnostic test, run detail smoke | Process success as task proof, full artifact shape, transient retry log as user action |
| Development v3 reset | v3 is a development reset. Local v1/v2 DB/state and old managed adapter files are not migrated in place; a legacy DB fails closed with a reset instruction. `loopora dev reset --workdir <project>` previews Loopora-owned project state, Loopora-managed adapter files, and the local app DB by default; `--yes` performs deletion while skipping unowned host files. | `db_schema.py`, `dev_reset.py`, `cli_dev_commands.py`, `agent_adapter_managed_files.py` | DB fresh/reject tests, dev reset dry-run/apply CLI test, adapter manifest/version tests | Production migration, deleting unowned host files, preserving old JSON/DB shapes as stable v3 contracts |
| Workflow | The default user model is linear and explainable: `Builder -> optional Inspector/Guide -> GateKeeper`. Advanced parallel/control fields are compatibility or expert-only surfaces. | `workflows.py`, workflow runtime | default preset tests, minimal advanced negative/compat checks | Parallel/control as default fixture or journey path |
| Agent Native | Agent Native lets the current Coding Agent remain the execution subject while Loopora owns planning/review, run-stage recovery, host-native role dispatch, evidence handoffs, submit acceptance, and task-proof/lifecycle separation. `/loopora-plan` creates, revises, repairs, or tightens reviewed Loop previews; `/loopora-run` starts, resumes, replays, or continues evidence under that reviewed Loop. Compact JSON/plain summaries expose the minimal native surface needed before opening full payloads: explicit activation, host-native dispatch, workspace/worktree ownership, project-local packaging, context loading, health checks, recovery, path-based handoff, permission/tooling/observability boundaries, experience-capability hints, a first-screen `agent_work_panel`, and proof/lifecycle source hints. Real-agent phase reports may add `diagnostics.experience_health` for review-only signals that the host saw work panel, todo, question, dispatch, native trace, or auto-repair guidance; those signals are never task proof. Field-level current-step, terminal-proof, submit-repair, blocked-action, evidence-handoff, and adapter-install details are owned by the narrower rows below plus contract tests, not by this umbrella row. Templates/tests inherit host model/provider defaults unless explicitly supplied, while model/provider routing, external routers, marketplaces, symlinks, host memory, injected editor context, host entry reload, behavior rules, todos, native traces, and credentials remain host-owned hints rather than Loopora policy or proof. | `agent_adapters.py`, `agent_native_*`, `service_agent_native.py`, `service_agent_native_contracts.py`, `cli_agent_*` | adapter entry validator, native capability contract test, capsule/submit contract, submit-flow and iteration-transition state tests, plan/run/next/submit work-panel/native-surface and terminal-proof tests, recovery-token tests, generated command home-preservation test, no hardcoded model/effort, opt-in real-agent probe and phase-report contract test | Nested provider CLI execution, Loopora-owned worktree switching, Loopora-owned approval-mode switching, Loopora-owned MCP/tool installation, implicit multi-agent fan-out, host behavioral auto-activation or rule injection as Loopora dispatch proof, remote marketplace or symlink state as runtime proof, Loopora-selected model/provider routing, host credentials as task proof, large inline prompt handoff payload, host session auto-takeover, exact prose or complete JSON shape, todo state, experience-health signal, or native trace as task evidence |
| Agent Native command namespace | `/loopora-plan` and `/loopora-run` are the only Loopora phase entries. Generic host or plugin workflow slash commands may coexist, but they are host-owned and must not be treated as Loopora plan/run phases or silently change Loopora phase semantics unless they invoke explicit Loopora CLI commands. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `agent_adapters.py`, `agent_adapter_templates.py` | native capability contract test, adapter install/native-surface tests, public docs test | Generic `/plan`, `/review`, or workflow aliases as Loopora phases; host plugin command namespace ownership |
| Agent Native context loading | Agent entries load summaries first and open reference paths or full payloads on demand. Host memory, host auto-compact or manual compact summaries, host-loaded skills/commands/agents, editor/IDE-injected files, selections, diagnostics, cursors, file mentions, marketplace catalogs, and uninstalled plugin components can guide the host Agent, but they are hints rather than Loopora context binding, role contracts, or task proof. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `cli_agent_adapter_output.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating host memory, compact summaries, catalogs, uninstalled components, host-loaded skills/commands/agents, or editor/IDE-injected context as Loopora binding, role contract, or evidence |
| Agent Native external memory stores | External persistent memory stores, semantic indexes, and memory MCP servers may help the host Agent recall context, but they remain host-owned hints rather than Loopora context binding, role contracts, or task evidence. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `cli_agent_adapter_output.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating retrieved memories, vector indexes, or memory MCP outputs as Loopora binding, role contracts, or evidence without explicit submission |
| Agent Native workflow kits | Host spec-driven workflows, PRD packs, quality-gate recipes, workflow kits, plugin suites, command libraries, phase-command recipes, rules, workflow templates, playbooks, and dynamic prompts may guide the Agent, but they are not a reviewed Loop workflow, Loopora install proof, or task evidence. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `cli_agent_adapter_output.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating host spec workflows, PRD packs, quality-gate recipes, workflow kits, command templates, playbooks, dynamic prompts, or plugin-suite recipes as reviewed Loop workflow contracts, install proof, or task evidence |
| Agent Native role catalogs | External agent catalogs, subagent libraries, and role marketplaces may help the host Agent choose expertise, but they are selection hints rather than Loopora role contracts, target-role dispatch, model routing, or permission policy. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `cli_agent_adapter_output.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating external role catalogs, subagent libraries, or role marketplaces as Loopora role contracts, dispatch targets, model policy, or permission policy |
| Agent Native shadow entries | Stale, duplicate, or shadowing host entries are entry-visibility risks rather than Loopora proof. Checks may report them, but repair/removal stays limited to entries with provable Loopora-managed ownership. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `agent_adapters.py`, `agent_adapter_static_checks.py` | native capability contract test, adapter install/native-surface tests, managed obsolete cleanup and conflict tests, public docs test | Global or legacy duplicate entries as Loopora proof; deleting unowned host entries; using shadow state to infer the active Loopora role contract |
| Agent Native generated projections | Generated runtime projections, plugin caches, marketplace metadata, and host sidecar files may help discovery or installation checks, but Loopora behavior is canonical only from Loopora Core plus managed references. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `agent_adapter_templates.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating generated projections, plugin caches, marketplace metadata, or sidecar files as canonical Loopora behavior |
| Agent Native lossy exports | Prompt-only, wrapper-only, cross-host portable skill exports, or external skill-installer outputs may guide the host Agent, but they do not prove adapter-native commands, role agents, hooks, managed references, or Loopora install proof unless adapter checks verify those components. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `agent_adapter_templates.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating prompt-only, wrapper-only, portable cross-host exports, or external skill-installer outputs as native capability parity or install proof |
| Agent Native hook runners | External or compatibility hook runners are opt-in host automation. Loopora must not enable them, depend on them, or treat their lifecycle output as Loopora dispatch or task proof. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `agent_adapter_host_config.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating hook runners as Loopora-owned native dispatch, cross-host hook parity, or task evidence |
| Agent Native statusline metrics | Host statusline usage, cost, context-window, model, cache, MCP, memory, and git metrics may improve situational awareness, but they are observation signals rather than Loopora progress, dispatch, or task proof. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `cli_agent_adapter_output.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating statusline usage/cost/context/git/model metrics as Loopora progress, dispatch, or task evidence |
| Agent Native diagnostic traces | External trace viewers, API proxies, session recorders, and usage analyzers may help diagnose host Agent behavior, but they are diagnostic material rather than Loopora evidence, coverage, or verdict. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `cli_agent_adapter_output.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating trace viewers, API proxy logs, session recordings, or usage analyzers as Loopora evidence, coverage, or verdicts without explicit submission |
| Agent Native task trackers | External task managers, todo boards, backlogs, issue trackers, and subagent status panels may coordinate host work, but they are not Loopora run lifecycle or task proof unless their outputs are submitted as Loopora evidence and judged by the task verdict. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `cli_agent_adapter_output.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating external task status, todo completion, backlog movement, issue state, or subagent status as Loopora lifecycle or proof |
| Agent Native remote controls | GitHub Actions workflows, PR bots, issue/PR comment triggers, remote dashboards, messaging bots, webhooks, cron jobs, and background session consoles may help users supervise or trigger host Agents, but they are host control surfaces rather than Loopora activation, role dispatch, or task proof. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `cli_agent_adapter_output.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating CI actions, PR bots, issue/PR comment triggers, remote control panels, bots, webhooks, cron triggers, or background consoles as Loopora activation, dispatch, or task evidence |
| Agent Native permission automation | Auto-approvers, full-access modes, and no-sandbox or sandbox-bypass launchers are host opt-in automation. Loopora must not enable, require, or treat them as dispatch or task proof. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `agent_adapter_templates.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating auto-approval, full-access, or sandbox bypass as Loopora policy, native dispatch, or evidence |
| Agent Native sandbox runners | External containers, devcontainers, MicroVMs, and remote runners may isolate host Agent work, but they are host isolation surfaces rather than Loopora workspace ownership, permission policy, or task proof. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `cli_agent_adapter_output.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating containerized, devcontainer, MicroVM, or remote-runner state as Loopora workspace ownership, permission policy, or task evidence |
| Agent Native security guardrails | External security scanners, guardrails, and Agent firewalls may help enforce host policy, but they are host controls rather than Loopora permission policy or task evidence. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `cli_agent_adapter_output.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating external security scanners, guardrail middleware, or Agent firewalls as Loopora permission policy or task evidence |
| Agent Native external orchestration | External swarms, party modes, and plugin orchestrators may guide host collaboration, but they are not Loopora parallel groups or role-dispatch proof unless the reviewed Loop workflow declares that handoff. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `agent_native_parallel_groups.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating external orchestrators, party modes, or swarms as Loopora-owned role dispatch or parallel contracts |
| Agent Native checkpoint restore | Host checkpoint, rewind, restore-point, and session-archive tools may help recover workspace or transcript context, but they are recovery hints rather than Loopora context binding, role dispatch, or task evidence. | `agent_native_adapter_contracts.py`, `agent_native_surface.py`, `cli_agent_adapter_output.py` | native capability contract test, adapter install/native-surface tests, public docs test | Treating restored files, rewind state, checkpoint metadata, or archived sessions as Loopora binding, dispatch proof, or evidence |
| Agent Native current-step recovery | When `loopora agent <adapter> next` cannot resolve the current host context binding but the workdir contains recoverable Loopora contexts, it must return structured recovery instead of a raw conflict. JSON starts with `agent_next_recovery_summary`; active-run choices expose a direct `next_agent_command` using the linked run id, while READY preview choices still route through `/loopora-run` or its CLI equivalent. | `cli_agent_native.py`, `cli_agent_context_recovery_output.py`, `service_agent_adapters.py` | agent-next no-binding recovery test, recoverable-context command/hint tests | Guessing the newest run, silently rebinding the host context |
| Agent Native submit repair | Recoverable submit failures must explain both sides of the mismatch before asking the Agent to edit JSON. Plain and JSON repair output expose the active result template, active filled-result path when known, result outbox when known, exact known ids, compact active known-evidence classifications, and active coverage target ids so the Agent can copy the template, save the filled copy to the right file, cite only valid evidence, and avoid inventing target IDs. The CLI may automatically repair only mechanical wrapper mistakes that can be recovered from the active result template, while Core validation still blocks unknown evidence refs, unknown coverage targets, target/actual-agent mismatch, inline execution, schema mismatch, and stale steps. If automatic wrapper repair happened before Core rejected the submit, repair output must say the format was repaired, preserve `auto_repair_actions`, and classify the surviving Core blocker with a stable `core_blocker_kind`. JSON starts with the v3 envelope and puts repair semantics in `agent_v3_envelope.summary`; old summary payloads are diagnostics under `raw.legacy`. The summary includes compact native-surface hints, includes the submitted file's `loopora_host_dispatch` summary when present, and points stale-step recovery to the active template, active filled-result path, active step, exact known ids, compact active known-evidence classifications, and the current schema lookup command while preserving an explicit workdir when one was used. | `agent_native_submit_validation.py`, `agent_native_v3.py`, `cli_agent_submit_auto_repair.py`, `cli_agent_submit_repair.py`, `cli_agent_submit_repair_output.py`, `cli_agent_submit_repair_schema.py`, `service_agent_native.py`, `service_agent_native_contracts.py` | host-dispatch submit repair tests, submit auto-repair success/blocker tests, stale-step and unknown-evidence submit repair tests, unfilled result template repair tests, v3 envelope contract tests, plain repair output test | Raw conflict-only errors, guessing stale step identity from filenames, using auto-repair to relax Core submit validation, hiding Core blockers after a wrapper auto-repair |
| Agent Native blocked actions | Blocked submit and iteration-repair summaries must not surface placeholder or contradictory next actions such as `None` or `No action needed`. When GateKeeper evidence-gate blockers or contract target IDs are present, compact JSON and plain output derive a concrete repair action or point users to `required_coverage` / `top_coverage_gaps` instead of surfacing bare codes. | `cli_agent_step_presenters.py`, `cli_agent_submitted_step_output.py`, `service_agent_native_contracts.py`, `service_agent_native.py` | submitted blocking item tests, iteration repair focus tests, contract-target blocker explanation test | Trusting blocked role-provided next action text over blocker semantics |
| Agent Native evidence handoff | Current-step summaries must show not only citable evidence IDs but also bounded latest evidence IDs, bounded evidence-ref summaries with role/result, GateKeeper support classification, support reason, claim, coverage target IDs, compact proof/artifact paths, top coverage gaps when present, and a coverage-classification note when citable evidence exists before any coverage target has been classified or when newly citable supporting evidence has not yet been reclassified after older weak/blocked coverage. That note must disappear once the only remaining gap is `gatekeeper.finish`, because the next GateKeeper verdict is the expected closure path rather than an unclassified Builder-evidence problem. Successful submit summaries show the submitted role's own coverage classifications and counts before the next-step handoff, so the user can see what the just-finished role changed without opening ledger artifacts, and coverage aggregation preserves explicit missing classifications as missing gaps rather than blockers. When a blocked GateKeeper opens a new iteration, compact JSON/plain output and the result template's `loopora_result_contract` must also expose `iteration_repair` source, blocking items, evidence refs, top gaps, and the recommended next action, while blocked coverage gaps retain the blocker evidence ref as well as cited upstream refs and reconcile target-specific blockers against current top gaps so resolved prior blockers do not remain as active instructions. The compact handoff must make blocked or non-supporting evidence visible before a role opens the full capsule/template, without embedding the full ledger, making the citable ID window disagree with evidence refs, or implying that unclassified Builder evidence is already target coverage. | `agent_native_capsule_context.py`, `agent_native_coverage_summary.py`, `agent_native_evidence_refs.py`, `agent_native_next_step_summary.py`, `agent_native_result_template.py`, `agent_native_submitted_step.py`, `cli_agent_current_step_output.py`, `cli_agent_step_presenters.py`, `cli_agent_submitted_step_output.py`, `cli_agent_submit_repair.py`, `service_agent_native_contracts.py`, `service_agent_native.py`, `context_flow.py`, `evidence_coverage.py` | submitted coverage summary test, explicit-missing coverage result test, blocked-target blocker-ref test, unclassified-new-evidence coverage note test, gatekeeper-only gap note suppression test, agent-next known-evidence-ref summary test, current-step known-evidence plain output test, GateKeeper support classification tests, iteration-repair summary tests, result-template iteration-repair test | Full ledger dump, using support labels as final verdicts |
| Adapter compatibility | Loopora only owns `.loopora/` state, Loopora-managed host entry files, project-local role configs, managed references, and the marked Claude session-context hook when that adapter is installed. Public adapter names stay `loopora-*`; removed managed entries are deleted only when ownership is provable. Install/check output must expose copyable next-step commands, diagnostic checks, first-task guidance, compact native-surface inventory, context identity, submit/no-nested boundary, and proof boundary without implying that host refresh, marketplace sync, symlink state, model routing, credentials, or prompt-only copies are Loopora proof. Managed `/loopora-plan` and `/loopora-run` entries stay thin, host-discoverable, and explicit; managed entries change only through check/init repair paths. Adapter checks distinguish pre-install state from installed drift, fail closed on user-owned conflicts, and keep `loopora agent <adapter> check` as the friendly runtime-namespace alias. | `agent_adapters.py`, `agent_adapter_*`, CLI init/uninstall | non-managed conflict tests, managed obsolete cleanup, namespaced-entry/no-model-default tests, install JSON next-step/native-surface test, entry frontmatter contract check, native-run entry contract tests, pre-install check recovery tests, missing role-agent config check, Codex role-TOML/contract check, Claude role-frontmatter/tool check, OpenCode role-permission boundary check, agent-check alias test | Overwriting host config, generic command/tool names, unowned uninstall cleanup, cross-host hook protocol parity assumptions, global skill sync as visibility proof, remote marketplace availability as runtime dependency, Loopora-selected model/provider routing, host credentials as task proof, large inline prompt handoff payload |
| Web Composer | Web supports conversation, compile, READY review, edit/improve, YAML import/export, create run, and run/evidence/verdict observation. | Web routes/templates/static pages | route availability, READY preview, create-run, run detail journey | CSS, DOM hierarchy, static wording, pixel layout |
