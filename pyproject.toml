[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "loopora"
version = "0.1.0"
description = "Local-first platform for long-running AI Agent tasks."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "jinja2>=3.1.0",
  "markdown>=3.6",
  "python-multipart>=0.0.9",
  "PyYAML>=6.0",
  "typer>=0.12.0",
  "uvicorn>=0.30.0",
]

[dependency-groups]
dev = [
  "httpx>=0.27.0",
  "playwright>=1.55.0",
  "pytest>=8.3.0",
  "ruff>=0.8.0",
]

[project.scripts]
loopora = "loopora.cli:app"

[tool.setuptools]
include-package-data = true

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]
include = ["loopora*"]

[tool.setuptools.package-data]
loopora = [
  "templates/*.html",
  "templates/partials/*.html",
  "static/*.css",
  "static/*.js",
  "static/pages/*.js",
  "static/pages/*.css",
  "assets/logo/*.svg",
  "assets/prompts/*.md",
  "assets/spec_practices/*.md",
  "assets/alignment/*.md",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "journey: local journey check coverage against in-process Loopora Web or local browser fixtures",
  "real_cli: opt-in real-environment probe that uses a real provider CLI for the minimal provider adapter boundary",
  "real_agent: opt-in real-environment probe that drives a real Coding Agent host or slash/skill command template",
  "release_web: opt-in real-environment probe against a real loopora serve process",
  "real_workflow_experiment: opt-in heavy real-provider workflow experiments that preserve artifacts and are not part of the release real-probe gate",
]

[tool.ruff]
line-length = 160
target-version = "py311"

[tool.ruff.lint]
select = [
  "E9",
  "F",
  "B",
  "ARG",
  "BLE",
  "C901",
  "FBT",
  "PERF",
  "PIE",
  "PLR0911",
  "PLR0912",
  "PLR0913",
  "PLR0915",
  "PLW",
  "RET",
  "RUF",
  "SIM",
  "UP",
]
ignore = [
  # Loopora intentionally includes Chinese product copy and examples.
  "RUF001",
]

[tool.ruff.lint.per-file-ignores]
# Typer command modules expose the public CLI flag surface in callback
# signatures; internal helpers must keep using structured request objects.
"src/loopora/cli_agent_adapter_commands.py" = ["FBT002", "PLR0913"]
"src/loopora/cli_agent_native.py" = ["PLR0913"]
"src/loopora/cli_agent_submit_repair.py" = ["PLR0913"]
"src/loopora/cli_diagnose_commands.py" = ["FBT001", "FBT003"]
"src/loopora/cli_loop_commands.py" = ["FBT002", "PLR0913"]
"src/loopora/cli_orchestration_commands.py" = ["PLR0913"]
"src/loopora/cli_role_commands.py" = ["PLR0913"]
"src/loopora/cli_root_commands.py" = ["FBT001", "FBT002", "FBT003", "PLR0913"]
"src/loopora/cli_spec_commands.py" = ["FBT002"]
"src/loopora/cli_shared.py" = ["F403"]
"tests/checks/contracts/agent_adapter_helpers.py" = ["F401"]
"tests/checks/contracts/test_agent_adapter_*.py" = ["F403", "F405"]
"tests/checks/contracts/test_agent_bundle_candidates.py" = ["F403", "F405"]
"tests/checks/contracts/test_agent_native_*.py" = ["F403", "F405"]

[tool.uv]
package = true
